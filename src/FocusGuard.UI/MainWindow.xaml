<Window x:Class="FocusGuard.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:FocusGuard.UI.Converters"
        Title="FocusGuard" Height="720" Width="960" MinHeight="600" MinWidth="780"
        Background="{DynamicResource BgBrush}">
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="20"/>
        </Style>
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
        <Style x:Key="RemoveButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
        </Style>
        <Style x:Key="Muted" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,28">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="FocusGuard" FontSize="25" FontWeight="SemiBold"/>
                <TextBlock Text="Keep only the tools you need." Style="{StaticResource Muted}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <ItemsControl ItemsSource="{Binding Sections}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding}"
                                    Style="{StaticResource NavButton}"
                                    Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                      CanContentScroll="False" PanningMode="Both" Focusable="False">
            <Grid>
                <StackPanel Visibility="{Binding IsFocusSelected, Converter={StaticResource BoolToVisibility}}">
                    <Border Style="{StaticResource Card}" Margin="0,0,0,16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="{Binding ActiveSessionName}" FontSize="18" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding SessionStatusText}" Style="{StaticResource Muted}" Margin="0,5,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding RemainingText}" FontSize="30" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="{DynamicResource AccentLightBrush}"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                                    <TextBox Text="{Binding DurationMinutes, UpdateSourceTrigger=PropertyChanged}" Width="62" HorizontalContentAlignment="Center"/>
                                    <TextBlock Text="minutes" Style="{StaticResource Muted}" VerticalAlignment="Center" Margin="4,0,10,0"/>
                                    <Button Content="Start focus" Command="{Binding StartSessionCommand}"/>
                                    <Button Content="End" Command="{Binding EndSessionCommand}" Background="{DynamicResource DangerBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Allowed apps" FontSize="20" FontWeight="SemiBold"/>
                            <TextBlock Text="Tick the apps you want to keep open. Windows and background services are excluded." Style="{StaticResource Muted}" Margin="0,4,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                            <Button Content="Find running apps" Command="{Binding AddRunningApplicationsCommand}" Background="{DynamicResource SecondaryBrush}"/>
                            <Button Content="Add app" Command="{Binding AddApplicationCommand}"/>
                        </StackPanel>
                    </Grid>

                    <Border Style="{StaticResource Card}">
                        <ListBox ItemsSource="{Binding SelectedApplications}" BorderThickness="0" MinHeight="260" MaxHeight="460"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Detail}" Style="{StaticResource Muted}" TextTrimming="CharacterEllipsis" MaxWidth="560"/>
                                        </StackPanel>
                                        <Button Grid.Column="2" Content="Remove" Style="{StaticResource RemoveButton}"
                                                Command="{Binding DataContext.RemoveApplicationCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}" VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>
                    <TextBlock Text="{Binding NoticeText}" Style="{StaticResource Muted}" Margin="0,14,0,0"/>
                </StackPanel>

                <StackPanel Visibility="{Binding IsWebsitesSelected, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Text="Allowed websites" FontSize="20" FontWeight="SemiBold"/>
                    <TextBlock Text="Website blocking needs the FocusGuard browser extension. It blocks every website except the ones you tick below." Style="{StaticResource Muted}" Margin="0,5,0,18" TextWrapping="Wrap"/>
                    <Border Style="{StaticResource Card}">
                        <DockPanel LastChildFill="True">
                            <Grid DockPanel.Dock="Top" Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding NewWebsite, UpdateSourceTrigger=PropertyChanged}" ToolTip="Example: github.com"/>
                                <Button Grid.Column="1" Content="Add website" Command="{Binding AddWebsiteCommand}" Margin="8,0,0,0"/>
                            </Grid>
                            <ListBox ItemsSource="{Binding SelectedWebsites}" BorderThickness="0" MinHeight="260" MaxHeight="460"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"/>
                                                <TextBlock Text="{Binding Detail}" Style="{StaticResource Muted}"/>
                                            </StackPanel>
                                            <Button Grid.Column="2" Content="Remove" Style="{StaticResource RemoveButton}"
                                                    Command="{Binding DataContext.RemoveWebsiteCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                    CommandParameter="{Binding}" VerticalAlignment="Center"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </DockPanel>
                    </Border>
                    <TextBlock Text="{Binding NoticeText}" Style="{StaticResource Muted}" Margin="0,14,0,0"/>
                </StackPanel>

                <StackPanel Visibility="{Binding IsSettingsSelected, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Text="Settings" FontSize="20" FontWeight="SemiBold"/>
                    <Border Style="{StaticResource Card}" Margin="0,18,0,0">
                        <StackPanel>
                            <TextBlock Text="Browser blocking" FontWeight="SemiBold"/>
                            <TextBlock Text="Install the FocusGuard browser extension to block websites. Desktop focus still works without it." Style="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,6,0,18"/>
                            <TextBlock Text="Local data" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding DatabaseLocation}" Style="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
